import HashSource from '../../HashSource';
import HashableImageSouceConverter from '../HashableImageSourceConverter';
import Resizer from '../Resizer/Resizer';
declare type GlayScaleCalculator = (r: Uint8, g: Uint8, b: Uint8) => Uint8;
export default class VanilaConverter implements HashableImageSouceConverter {
    private document;
    private glayScaleCalculator;
    private resizer;
    constructor(document: Document, glayScaleCalculator: GlayScaleCalculator, resizer: Resizer);
    private createCanvasRenderingContext2D;
    convert(source: HashSource): Promise<Uint8ClampedArray>;
}
export {};
