{
  "name": "zero-to-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "zero-to-app",
    "apps/*"
  ],
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "dev:storybook": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-storybook storybook",
    "dev:storybook:web": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-storybook storybook:web",
    "dev:demo": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-showcase start",
    "dev:demo:ios": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-showcase ios",
    "dev:demo:android": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-showcase android",
    "dev:demo:web": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-showcase web",
    "build": "pnpm --filter zero-to-app run build",
    "build:storybook": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-storybook build-storybook",
    "build:demo": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-showcase export:web",
    "build:all": "pnpm build && pnpm build:storybook && pnpm build:demo",
    "deploy:demo": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-showcase publish",
    "deploy:demo:preview": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-showcase publish:preview",
    "release": "pnpm run build && pnpm --filter zero-to-app publish --access public --no-git-checks",
    "lint": "pnpm --filter zero-to-app-showcase lint",
    "lint:fix": "pnpm --filter zero-to-app-showcase lint -- --fix",
    "help": "node -e \"console.log('\\n📦 Available Scripts\\n\\n🔧 Development:\\n  pnpm dev:storybook          Start Storybook (native Expo)\\n  pnpm dev:storybook:web       Start Storybook web dev server\\n  pnpm dev:demo               Start demo app\\n  pnpm dev:demo:ios            Start demo on iOS\\n  pnpm dev:demo:android        Start demo on Android\\n  pnpm dev:demo:web            Start demo on web\\n\\n🏗️  Build:\\n  pnpm build                   Build zero-to-app package\\n  pnpm build:storybook         Build Storybook static files\\n  pnpm build:demo              Build demo web export\\n  pnpm build:all               Build everything (package + storybook + demo)\\n\\n🚀 Deploy:\\n  pnpm deploy:demo             Publish demo OTA update (production)\\n  pnpm deploy:demo:preview     Publish demo OTA update (preview)\\n\\n📦 Release:\\n  pnpm release                 Publish zero-to-app package to npm\\n\\n✨ Quality:\\n  pnpm lint                    Lint code\\n  pnpm lint:fix                Lint and fix issues\\n\\n')\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pnpm": {
    "overrides": {
      "react-docgen-typescript": "2.2.2"
    }
  }
}
