{
  "name": "zero-to-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "zero-to-app",
    "apps/*"
  ],
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "dev:storybook": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-storybook storybook",
    "dev:storybook:web": "pnpm --filter zero-to-app build && pnpm --filter zero-to-app-storybook storybook:web",
    "dev:demo": "pnpm --filter zero-to-app build && pnpm --filter demo start",
    "dev:demo:ios": "pnpm --filter zero-to-app build && pnpm --filter demo ios",
    "dev:demo:android": "pnpm --filter zero-to-app build && pnpm --filter demo android",
    "dev:demo:web": "pnpm --filter zero-to-app build && pnpm --filter demo web",
    "build": "pnpm --filter zero-to-app run build",
    "build:demo": "pnpm --filter zero-to-app build && pnpm --filter demo export:web",
    "deploy:demo": "pnpm --filter zero-to-app build && pnpm --filter demo publish",
    "deploy:demo:preview": "pnpm --filter zero-to-app build && pnpm --filter demo publish:preview",
    "release": "pnpm run build && pnpm --filter zero-to-app publish --access public --no-git-checks",
    "lint": "pnpm --filter demo lint && pnpm typecheck",
    "lint:fix": "pnpm --filter demo lint -- --fix",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "clean": "rm -rf zero-to-app/dist apps/*/dist apps/*/.expo",
    "help": "node -e \"console.log('\\n📦 Available Scripts\\n\\n🔧 Development:\\n  pnpm dev:storybook          Start Storybook (native Expo)\\n  pnpm dev:storybook:web       Start Storybook web dev server\\n  pnpm dev:demo               Start demo app\\n  pnpm dev:demo:ios            Start demo on iOS\\n  pnpm dev:demo:android        Start demo on Android\\n  pnpm dev:demo:web            Start demo on web\\n\\n🏗️  Build:\\n  pnpm build                   Build zero-to-app package\\n  pnpm build:demo              Build demo web export\\n\\n🚀 Deploy:\\n  pnpm deploy:demo             Publish demo OTA update (production)\\n  pnpm deploy:demo:preview     Publish demo OTA update (preview)\\n\\n📦 Release:\\n  pnpm release                 Publish zero-to-app package to npm\\n\\n✨ Quality:\\n  pnpm lint                    Lint code\\n  pnpm lint:fix                Lint and fix issues\\n  pnpm typecheck               Run TypeScript checks\\n  pnpm clean                   Remove build artifacts\\n\\n')\"",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pnpm": {
    "overrides": {
      "react-docgen-typescript": "2.2.2"
    }
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "lint-staged": {
    "apps/demo/**/*.{ts,tsx}": [
      "eslint --fix --config apps/demo/eslint.config.js"
    ],
    "*.{ts,tsx}": [
      "bash -c 'pnpm typecheck'"
    ]
  },
  "dependencies": {
    "@expo/metro-runtime": "6.1.2",
    "expo": "~55.0.0-preview.7",
    "react": "19.2.0",
    "react-native": "0.83.1"
  }
}
